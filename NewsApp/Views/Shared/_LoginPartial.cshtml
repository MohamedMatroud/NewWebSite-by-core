@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

@if (SignInManager.IsSignedIn(User))
{
    @*<li class="nav-item">
            <a  class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity.Name!</a>
        </li>*@

    <li class="dropdown">
        <a href="#"><span>Managements</span> <i class="bi bi-chevron-down"></i></a>
        <ul>
            <li> <a class="nav-link scrollto" asp-area="" asp-controller="Categories" asp-action="Index">Categories </a></li>
            <li> <a class="nav-link scrollto" asp-area="" asp-controller="News" asp-action="Index">News </a></li>
            <li> <a class="nav-link scrollto" asp-area="" asp-controller="Teammembers" asp-action="Index"> Teammembers </a></li>

        </ul>
    </li>

    
    <li class=" nav-link scrollto">

        <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            @*<button type="submit" class="nav-link btn btn-link text-dark">Logout</button>*@
            <button type="submit"  class="appointment-btn scrollto"><span class="d-none d-md-inline">Logout</span> </button>

        </form>
    </li>
}
else
{
    @*<li class="">
            <a class="nav-link scrollto" asp-area="Identity" asp-page="/Account/Register">Register</a>
        </li>*@
<li class="">
    @*<a asp-area="Identity" asp-page="/Account/Login" class="appointment-btn scrollto"><span class="d-none d-md-inline">Login</span> </a>*@

    <a class="nav-link scrollto" asp-area="Identity" asp-page="/Account/Login">Login</a>
</li>
}

